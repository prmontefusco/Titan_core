"""Camada de aplicação da vertical Titan Asset & Sustainment (A4).

Casos de uso, portas (`Protocol`) e orquestração — nunca framework/ORM
(`docs/asset/DEPENDENCY_RULES.md` §1). Cresce incrementalmente por agregado —
nada aqui existe antes do agregado/serviço que o usa (constituição §38).
"""
